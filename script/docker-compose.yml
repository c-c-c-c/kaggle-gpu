version: "3"
services:
  kaggle-script:
    container_name: 'kaggle-script'
    image: 'okojoalg/kaggle-gpu:v0.1-36'
    volumes:
      - $PWD:/mount
    working_dir: /mount
    command: python /mount/run.py
